@model Maps.Models.vmMonster

@{
    ViewBag.Title = "Edit";
}

<h2>Edit #Monster</h2>
<br />
<br />

@using (Html.BeginForm("Edit", "Monster", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.ValidationSummary(true)

    @Html.HiddenFor(model => model.Monster_ID)
    
    <fieldset>
        <legend>Map</legend>
        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Avatar_File)
        </div>
        <div class="editor-field">
            <input type="file" id="Avatar_File" name="Avatar_File" />
            @Html.ValidationMessageFor(model => model.Avatar_File)
        </div>
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index", "Monster")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
